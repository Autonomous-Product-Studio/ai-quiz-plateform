# Contributing to AI Quiz Plateform

> Generated by Autonomous Product Studio (APS) - fill in the TODOs to ship.

1. Fork & branch from `main`.
2. Implement a `TODO:` (start with the open issues — each maps to a backlog item).
3. `pip install -r backend/requirements.txt && pytest backend`.
4. Open a PR referencing the issue.
